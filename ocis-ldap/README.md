## oCIS server with ldap

Server version: `owncloud/ocis:latest`

### Start the server

```bash
docker compose up
```

Server avaliable at `https://localhost:9200`

### Stop the server

```bash
docker compose down -v
```
